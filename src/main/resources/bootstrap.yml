spring:
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: http
      token: abc123
      mongodb:
        enabled: true
        role: readonly
        backend: database
        username-property: mongodb.username
        password-property: mongodb.password
  application:
    name: sample

logging:
  level:
    org.springframework: DEBUG


